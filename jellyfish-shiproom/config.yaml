# Configuration file - credentials come from environment variables (.env file)
# Non-sensitive configuration goes here

# Required: Google Slides presentation to update
google_slides:
  presentation_id: "1zua6Ks2fAJPtv7utIOffN1WPoStNcrUGDEn2pewKoig"
  # slide_id is now configured per team in teams_config.yaml

# Teams Configuration
teams_config_file: "teams_config.yaml"

# Team Selection
# Use "all" to generate slides for all teams, or specify a team identifier (e.g., "BBEE", "TOL", "CLIP")
team_selection: "all"  # Options: "all" or specific team identifier like "BBEE"

# Legacy single-team configuration (deprecated - use teams_config.yaml instead)
# team:
#   team_id: "90301"
#   team_name: "Team Bumblebees"

# Optional: Override default status mappings
status_mapping:
  in_progress: ["In Progress", "Active", "Started"]
  done: ["Done", "Completed", "Resolved", "Closed"]

# Optional: Field mappings if different from defaults
field_mappings:
  deliverable:
    key_field: "key"
    name_field: "summary"
    due_date_field: "due_date"
    status_field: "status"
  epic:
    key_field: "key"
    name_field: "summary"
    due_date_field: "due_date"
    status_field: "status"




